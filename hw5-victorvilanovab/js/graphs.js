class Graphs {
  constructor(containerElement) {
    this.containerElement = containerElement;

    const section_grafics = document.createElement("section");
    section_grafics.className = "graphics";
    this.containerElement.appendChild(section_grafics);


  }
}